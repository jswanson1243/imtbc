<%= form_for(@article, :html => {class: "form-horizontal", role: "form"}) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class='row'>
  <div class='col-xs-10'>
    <div class="field">
      <div class="form-group">
        <div class="control-label col-sm-2">
          <%= f.label :title %>
        </div>
      <div class="col-sm-10">
        <%= f.text_field :title, class: "form-control", placeholder: "Title of Trip Report", autofocus: true %>
    </div>
  </div>
</div>
  <div class="field">
   <div class="form-group">
     <div class="control-label col-sm-2">
      <%= f.label :description %>
     </div>
     <div class="col-sm-10">
      <%= f.text_area :description, rows: 10, class: "form-control", placeholder: "Body of Report" %>
     </div>
    </div>
  </div>
  <div class="actions">
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
    <%= f.submit "Create Report", class: 'btn btn-primary btn-lg' %>
      </div>
    </div>
  </div>
<% end %>
  </div>
</div>  